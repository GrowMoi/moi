.awards-page
  - content_for :title, t("views.awards.new")

  = nested_form_for [:admin, award] do |f|

    = render "admin/shared/form_errors", resource: award

    .form-group
      legend =t("views.awards.form_new.name")
      = f.text_field :name, class: "form-control"

    .form-group
      legend =t("views.awards.form_new.description")
      = f.text_area :description, class: "form-control"

    .form-group
      legend =t("views.awards.form_new.image")
      .row
        .col-xs-12
          .form-group
            = tooltip t("views.awards.form_new.attach_image"),
                      place: "left",
                      class: "btn btn-xs btn-default content-media-uploader" do
              .glyphicon.glyphicon-cloud-upload

            .hidden
              = f.file_field :image

            span.content-media-name-on-form
              - if f.object.image?
                = decorate(f.object).link_for_form

    .form-group
      legend =t("views.awards.form_new.settings")

      label =t("views.awards.form_new.settings_category")
      .settings-options-container.row-settings
        = select_tag  :award_category,
                      options_for_select(award_categories),
                      class: "category-selector"

      .aproved-content.row-settings
        label =t("views.awards.form_new.settings_aproved_content")
        .settings-options-container
          = select_tag  :award_aproved_content,
                        options_for_select(award_aproved_contents),
                        class: "aproved-content-selector"
      .question-number.row-settings
        label =t("views.awards.form_new.settings_question_number")
        .settings-options-container
          = number_field_tag  :award_question_number, 3, step: 3, class: "settings-number-field"

      .content-number.row-settings
        label =t("views.awards.form_new.settings_content_number")
        .settings-options-container
          = number_field_tag  :award_content_number, 1, class: "settings-number-field"

    .actions
      = f.submit t("views.awards.send"),
                class: "btn btn-primary",
                data: { disable_with: t("views.submitting")}

      = link_to t("views.awards.back"), admin_awards_path
